import { DataEnrolementComplet } from './data-enrolement-complet';

describe('DataEnrolementComplet', () => {
  it('should create an instance', () => {
    expect(new DataEnrolementComplet()).toBeTruthy();
  });
});
