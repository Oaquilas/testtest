import { ModelPossibleDoublons } from './model-possible-doublons';

describe('ModelPossibleDoublons', () => {
  it('should create an instance', () => {
    expect(new ModelPossibleDoublons()).toBeTruthy();
  });
});
