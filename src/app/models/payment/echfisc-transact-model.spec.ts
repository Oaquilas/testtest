import { EchfiscTransactModel } from './echfisc-transact-model';

describe('EchfiscTransactModel', () => {
  it('should create an instance', () => {
    expect(new EchfiscTransactModel()).toBeTruthy();
  });
});
