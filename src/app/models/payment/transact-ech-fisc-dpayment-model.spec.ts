import { TransactEchFiscDPaymentModel } from './transact-ech-fisc-dpayment-model';

describe('TransactEchFiscDPaymentModel', () => {
  it('should create an instance', () => {
    expect(new TransactEchFiscDPaymentModel()).toBeTruthy();
  });
});
