import { TransactEchFiscPaymentModel } from './transact-ech-fisc-payment-model';

describe('TransactEchFiscPaymentModel', () => {
  it('should create an instance', () => {
    expect(new TransactEchFiscPaymentModel()).toBeTruthy();
  });
});
