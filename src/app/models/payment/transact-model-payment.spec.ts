import { TransactModelPayment } from './transact-model-payment';

describe('TransactModelPayment', () => {
  it('should create an instance', () => {
    expect(new TransactModelPayment()).toBeTruthy();
  });
});
