import { ConfirmpaiementRetour } from './confirmpaiement-retour';

describe('ConfirmpaiementRetour', () => {
  it('should create an instance', () => {
    expect(new ConfirmpaiementRetour()).toBeTruthy();
  });
});
